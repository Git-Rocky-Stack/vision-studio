"""
Database migrations for Vision Studio.

Each migration file should be named with a three-digit prefix (e.g., 001_initial_schema.py)
and must implement:
    - migrate_up(conn): Apply the migration
    - migrate_down(conn): Rollback the migration
"""
